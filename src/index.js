import React from "react";
import ReactDOM from 'react-dom/client';
import ClassState from "./ClassState";

const el = document.getElementById('root');
const root = ReactDOM.createRoot(el);

root.render(<ClassState />);

